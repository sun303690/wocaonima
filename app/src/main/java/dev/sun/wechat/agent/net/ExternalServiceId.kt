package dev.sun.wechat.agent.net

/** Well-known service IDs used as primary keys in the `external_services` table. */
object ExternalServiceId {
    const val EXA = "exa"
    const val BRAVE = "brave"
}
